When creating a new command, be sure to add a hashtag to the end of the command line
so the custom help implementation can find it easier.

Example:
# Commands:
#   hubot command - description #hashtag
